#+title: T480s Bootstrap Handoff
#+startup: overview

* 2026-08-31 Generation 10 Yazi Acceptance
The accepted replacement began from synchronized =main= and =origin/main= at
=5cb04a9d98bf6d2251c1eda34732ffff24eaa46d=.  The tracked =flake.lock= was not
changed; its SHA-256 remains
=f3c0eace8401b44d3d9ea4cf774c8caff10165a6e258eed0e93f461ca03b440e=.

Ranger, =ranger_devicons=, their package module and their two live configuration
links were removed.  The native Home Manager =programs.yazi= module now owns
Yazi, its =y= shell wrapper, navigation settings and explicit nsxiv, lean-mpv and
Brave openers.  Fish =fm= launches =y=, =FILE_MANAGER= is =yazi=, the repository
tmux session launches Yazi, and tmux permits terminal image-protocol
passthrough.

Exactly one non-activating build completed with status 0:

#+begin_example
nix build --print-out-paths --no-link --no-write-lock-file path:/home/skydive420dz/Projects/t480s-dotfiles#nixosConfigurations.t480s.config.system.build.toplevel
/nix/store/y9h47jmsrj2n6x7gdwmvf3ap7akzyfhx-nixos-system-t480s-26.11.20260829.d2f6794.drv
/nix/store/q80fa9kixjr83ycjag9g9hwbnyjj0a18-nixos-system-t480s-26.11.20260829.d2f6794
#+end_example

The exact candidate was test-activated before persistent staging.  Runtime
checks found Yazi 26.8.15, no Ranger executable, the intended Fish and
environment ownership, tmux passthrough, Ghostty's image adapter, working image
preview locally and through SSH, and the exact nsxiv opener.  The user accepted
the nsxiv opener and Yazi behavior.

The candidate was then staged as generation 10 without rebooting.  After one
separately authorized reboot, current, booted and profile-selected system paths
all resolve to =q80fa9...=.  Generation 9 =ysxm6ik...= remains the retained
systemd-boot fallback.  No failed system or user units were observed.

The accepted implementation is commit =acff34d=; the pre-documentation
restricted diff has SHA-256
=54e402e67f6579cb2145384141c1d593a954bf12e7554c88098335063972573c=.

* Historical 2026-08-31 Fingerprint Resume Acceptance
The accepted baseline starts from =main= at
=541ceecb7b1bcd9acbfb9b86427bd586fb686cea= plus the intentional
=flake.lock= refresh to nixpkgs =d2f6794= and Home Manager =82c265f=; the
=t480-fingerprint= input remains pinned at
=74f9899daf4a272160a9190fa5c72dfbe6c7fb57=.  The lock SHA-256 is
=f3c0eace8401b44d3d9ea4cf774c8caff10165a6e258eed0e93f461ca03b440e=.

After hybrid resume, USB reader =06cb:009a= re-enumerated while
=python3-validity.service= retained its old libusb handle.  The daemon remained
active, but the reader remained unusable until restart.  During restart, the
old daemon's exit callback confirmed its stale handle with =Errno 19=.  The
host-owned repair adds only:

#+begin_src nix
powerManagement.resumeCommands = "${pkgs.systemd}/bin/systemctl restart python3-validity.service";
#+end_src

Exactly one non-activating build completed with status 0:

#+begin_example
nix build --print-out-paths --no-link --no-write-lock-file path:/home/skydive420dz/Projects/t480s-dotfiles#nixosConfigurations.t480s.config.system.build.toplevel
/nix/store/4l0ji8nzvslxfpm7xjmja50q1ynh38al-nixos-system-t480s-26.11.20260829.d2f6794.drv
/nix/store/6mwkw2m603gy1l6nhksm47yw35hkbq7p-nixos-system-t480s-26.11.20260829.d2f6794
#+end_example

The generated =sleep-actions.service= contains exactly one post-resume restart
of =python3-validity.service=.  The candidate was test-activated without
changing the system profile or boot entry.  One hybrid-sleep cycle preserved
boot ID =a6d20bb3-638d-41fe-8007-39ae9cc4644b=; the reader disconnected and
re-enumerated, and the driver PID changed from =9489= to =13748=.  The new
daemon opened the sensor, loaded calibration from the existing file, retained
the enrolled =right-index-finger=, and passed physical sudo authentication with
=status=0= and no password fallback.  =open-fprintd.service= and
=python3-validity.service= are active, and no failed system units were observed.

The test-active candidate is =6mwkw2m...=.  The persistent profile and booted
generation remain generation 9 at
=/nix/store/ysxm6ikknlzfvqnkcrvmv3kjvwc2w28j-nixos-system-t480s-26.11.20260829.d2f6794=.
Persistent promotion and reboot acceptance remain separate gates.

* 2026-08-31 Home Artifact Cleanup
After a read-only inventory and explicit permanent-deletion confirmation, the
T480s removed 20 obsolete directories and 6 files totaling 4.1 GiB.  The set
contained completed gate-evidence directories, the superseded Gate 5C NAR,
abandoned Emacs migration/pre-repair state, old T480s logs, the Nix evaluation
cache, stale autosave indexes and broken legacy Nix profile/channel links.  The
raw artifacts are not recoverable.

Home usage fell from 6.1 GiB to 2.1 GiB.  The active 1.5 GiB Brave cache and
browser profile were explicitly excluded; SSH, projects, current configuration,
active Emacs state and authentication data were preserved.  The cleanup did not
change the repository, active system, persistent profile, booted generation or
failed-unit state.

* Historical Generation 7 Fingerprint Acceptance
The accepted fingerprint candidate is
=/nix/store/30v62h3svs3jl6a3rsyk2qc707nzapga-nixos-system-t480s-26.11.20260813.0e251e2=
with derivation
=/nix/store/mlnbiw93gmrs51hcbg3nn2gb0y6lqj1b-nixos-system-t480s-26.11.20260813.0e251e2.drv=.
At that checkpoint it was current and profile-selected.  Generation 6
=/nix/store/fx3mgcvkyzvswfzpszbnwbvqkzv057ij-nixos-system-t480s-26.11.20260813.0e251e2=
remained booted until a separately authorized reboot.

The host-owned fingerprint module follows
=github:viktor-grunwaldt/t480-fingerprint-nixos= through =flake.lock=, applies
local =06cb:009a= compatibility and runtime-state safety repairs, and enables
=open-fprintd= plus =python-validity=.  Only sudo PAM fingerprint
authentication is enabled; login and other PAM surfaces remain disabled and
password fallback is retained.

The exact =06cb:009a= reader has one enrolled =right-index-finger=.  Direct
fingerprint verification and sudo authentication passed; =open-fprintd.service=
and =python3-validity.service= are active, and no failed system units were
observed.  Browser WebAuthn is paused after a read-only security review; no
browser bridge, extension, native host or browser policy was installed.

* Generation 6 Journal Remediation Acceptance
Generation 6 was built from base HEAD
=8ef756086ecf88573596a4aa8e501b25a624d37b= with final source in:

- =host/t480s/system/hardware-configuration.nix=;
- =host/t480s/system/modules/services/x11.nix=;
- =host/t480s/system/modules/services/power.nix=.

The system derivation is
=/nix/store/3x58hs91qd1zh08yvihrhhlgc9a8pgh6-nixos-system-t480s-26.11.20260813.0e251e2.drv=,
and the accepted toplevel is
=/nix/store/fx3mgcvkyzvswfzpszbnwbvqkzv057ij-nixos-system-t480s-26.11.20260813.0e251e2=.
At that checkpoint, generation 6 was current, booted, profile-selected and the
systemd-boot default.
Generation 5 remains the listed, selectable fallback at
=/nix/store/h3pwl7ikpwk5xqjglg73kmzwsyrzv3ng-nixos-system-t480s-26.11.20260813.0e251e2=.

The accepted remedies restrict the writable VFAT ESP to
=fmask=0077,dmask=0077=; add an exact =Elan TrackPoint= rule for button
scrolling on button 2 after the generic mouse rule without changing the
touchpad policy; and use valid TLP AC/BAT CPU policy while leaving both
=DISK_IDLE_SECS_ON_*= values empty so TLP does not write the obsolete Linux
7.x =vm.laptop_mode= knob.  AC uses =performance/performance=; battery uses
=powersave/balance_power=.

Historical raw evidence was originally host-local and split by gate:

- build: =~/t480s-epp-build-evidence-20260816T223000-0400=;
- userspace/battery: =~/t480s-userspace-evidence-20260816T202300-0400=;
- focused AC activation: =~/t480s-ac-activation-evidence-20260816T224500-0400=;
- profile/boot staging: =~/t480s-stage-evidence-20260816T231000-0400=;
- fresh boot: =~/t480s-reboot-evidence-20260816T234300-0400=.

Those raw directories were permanently removed by the audited cleanup recorded
above after their accepted results were incorporated into this handoff and the
gate ledger.  Their paths below identify historical provenance, not live files.

The fresh boot ran Linux 7.1.8 with live =fmask=0077,dmask=0077=.
Ordinary-user access to =loader/random-seed= was denied and no insecure-seed
warning appeared.  TLP applied AC =performance/performance= on all eight
policies.  Xorg/DWM, TrackPoint button scrolling, the retained touchpad
two-finger/natural-scrolling policy, SSH and Wi-Fi passed automated and
physical acceptance; system and user failed-unit counts were zero.  The prior
test activation retained =0022= because VFAT ignores these mask changes during
remount; the fresh mount correctly applied =0077=.  Locked =nix run=
diagnostics were ephemeral tools, not permanent dependencies.

** Fresh-boot journal reconciliation
Generation 6 completed fresh-boot reconciliation with zero failed system or
user units.  The evidence was originally recorded at
=/home/skydive420dz/t480s-journal-reconciliation-evidence-20260817T003000-0400=.
The accepted repairs remained effective: no world-accessible =/boot= or random
seed warning, deprecated TLP =vm.laptop_mode= write, or TrackPoint
two-finger-scroll failure occurred; the touchpad's two-finger and natural
scrolling policy remained intact.

Duplicate system and user D-Bus activation names are an upstream/NixOS
packaging-path deduplication defect; the services themselves are healthy.  The
rfkill warning is the standard udev request meeting NixOS's intentional TLP
mask.  Do not unmask =systemd-rfkill= or create dual radio-policy ownership.
Firmware/hardware capability diagnostics remain bounded by their recorded
reopen criteria and are neither repaired nor candidates for suppression.

The =org.freedesktop.resolve1= requester remains a separate future trace.
NetworkManager and resolvconf own DNS; do not enable systemd-resolved merely to
remove that warning.  The WEXT caller also remains unproven; do not name Brave
or another application without contemporaneous attribution.

The user-manager I/O-pressure item is closed with result
=PASS — ATTRIBUTED, REDUNDANT ADJUSTMENT=.  Evidence was originally recorded at
=/home/skydive420dz/t480s-psi-attribution-evidence-20260817T010000-0400=.
All eight errno-16 records belonged to UID 1000, =user@1000.service= and
=init.scope=; manager PIDs =1894=, =2024=, =2135= and =2190= each emitted two.
The surviving PID =2190= had invocation
=cc728039e900422ab9deb757282fe79a=.

Effective policy was =IOPressureWatch=auto= with a 200 ms threshold.  The
inherited watch targeted
=/sys/fs/cgroup/user.slice/user-1000.slice/user@1000.service/io.pressure=, and
=IO_PRESSURE_WRITE= decoded strictly as data to =some 200000 2000000\0=: a
200 ms threshold over a 2 s window.  Installed systemd was 261.1.  Matching
official v261 source proved that the inherited watch is locked; manager
initialization and =init.scope= realization each request the identical policy;
and the period setter returns internal =EBUSY= before constructing or
performing another kernel write.  The inherited watch remained open and
pressure monitoring remained active.

This is a systemd 261 logging/integration defect, not external descriptor
contention, disabled PSI or a T480s runtime failure.  No local workaround is
warranted: do not disable or alter PSI, systemd-oomd, resource control or
pressure monitoring, and do not suppress the message.  Recheck after a future
systemd update.  The former user-manager I/O-pressure trace item is therefore
closed as an attributed upstream defect.  Primary-source anchors:
[[https://github.com/systemd/systemd/blob/v261/src/core/manager.c#L819-L837][manager setup]],
[[https://github.com/systemd/systemd/blob/v261/src/core/cgroup.c#L2180-L2191][init.scope setup]],
[[https://github.com/systemd/systemd/blob/v261/src/libsystemd/sd-event/sd-event.c#L1930-L2134][inherited watch handling]] and
[[https://github.com/systemd/systemd/blob/v261/src/libsystemd/sd-event/sd-event.c#L5453-L5518][locked-period guard]].

Battery policy acceptance comes from the earlier userspace gate; its generated
policy was unchanged by the later one-line AC EPP alignment.  Secure Boot
remains disabled with Setup Mode enabled.  This slice did not repair or
universally accept unrelated D-Bus, firmware, hardware, networking,
suspend/resume or other journal findings.  Generation 5 remains the fallback;
no generation deletion, garbage collection, further activation or additional
reboot occurred.

* Identity
- Source-work host: T480s NixOS at =skydive420dz@192.168.1.212=
- Repository: =/home/skydive420dz/Projects/t480s-dotfiles=
- Branch: =main=
- History: standalone history rooted in one parentless initial commit; no
  inherited commit is an ancestor
- Origin: =https://github.com/skydive420dz/t480s-dotfiles.git=
- Flake output: =nixosConfigurations.t480s=
- T430 source target: =skydive420dz@192.168.1.216=

The repository is standalone.  MSI is neither the current work host nor an
authorized source/edit target for this slice.

* Authorization Boundary
Historical results remain recorded below: Gate 1 source, Gate 2 build, Gate 3
storage/install, Gate 4 boot, Gate 5 health/correction activation, Gate 5B
candidate build and Gate 5C transfer.  They are not authorization or evidence
for later source bytes.

Those historical boundaries did not authorize later promotion or reboot.  The
later fingerprint and resume gates were separately authorized and are recorded
above and in [[file:gates.org][the gate ledger]].  This closeout is limited to the
intentional input refresh, the accepted one-line resume repair, these
current-state documents, scoped normal commits and one non-force push.  It does
not authorize MSI access, hardware regeneration, persistent promotion or
another runtime test.

* Current Source Slice
- =config/= owns live-editable application and desktop configuration outside
  the Nix store;
- =core/home/= and =core/system/= own machine-agnostic Home Manager and NixOS
  policy;
- =host/t480s/home/= and =host/t480s/system/= own T480s-specific user and
  system policy;
- import direction is =flake -> host -> core=;
- DWM, dmenu and slstatus headers are the compile-time =config/= exceptions;
- the exact T430 behavior is preserved with the approved Brave, Emacs and
  Ghostty substitutions.

Current Yazi source and fresh-boot status are recorded in =2026-08-31
Generation 10 Yazi Acceptance= above.  Current fingerprint status is recorded
in =Historical 2026-08-31 Fingerprint Resume Acceptance= and in
[[file:gates.org][the gate ledger]].  Older gate results below remain unchanged.

The historical Gate 5B build remains
=/nix/store/a86l514b3w24klc7jy09wj3cv46vy263-nixos-system-t480s-26.11.20260729.0954f7e=.
Its store-backed Emacs ownership is superseded by the approved live
=config/emacs/= source.  It was not activated and does not validate the current
repository baseline.  Its historical transfer artifact and checksum remain recorded
in [[file:gates.org][the gate ledger]].

* Gate 4 And Gate 5 Runtime Result
- The installed generation booted as =t480s= with Linux 6.18.40 and the exact
  Gate 2 toplevel.
- Root, =/home=, ESP, swap, and resume identities match the generated hardware
  configuration.
- systemd reported =running= and no failed system or user units.
- Wi-Fi at =192.168.1.212=, SSH, NetworkManager, Bluetooth, D-Bus, TLP, and
  UPower were active.
- PipeWire/WirePlumber discovered built-in analog sink/source after session
  initialization; physical playback and capture remain untested.
- TLP reported AC performance policy; BAT0 thresholds were 75/90.
- X is intentionally owned by manual =startx= and was not started through the
  remote TTY.  Desktop behavior remains Gate 6.
- Defect: =host/t480s/system/default.nix= redeclares BlueZ's shipped
  =mpris-proxy.service=, producing two =ExecStart= directives and
  =Loaded: bad-setting=.  The redundant host unit was removed.  The corrected
  7.8 GiB toplevel is
  =/nix/store/dz6wmbdjrrp5aa4025iyz1ppkfv2vgx1-nixos-system-t480s-26.11.20260729.0954f7e=;
  it contains only BlueZ's unit with one =ExecStart=, was the then-current
  system and remains the persistent profile.  At that checkpoint the unit
  loaded cleanly but remained disabled/inactive.

* Gate 3B Installation Result
- Installed generation 1 is
  =/nix/store/a1rglfnj8fpql25w301v5xwk05qvw266-nixos-system-t480s-26.11.20260729.0954f7e=.
- systemd-boot created EFI entries =Boot0015 Linux Boot Manager= and
  =Boot0016 Fallback Linux Boot Manager=.
- The loader entry references Linux 6.18.40, the matching initrd, and the
  verified swap resume UUID.
- The =skydive420dz= password was set interactively and verified only as set;
  no password or hash was recorded.
- AC remained online and the Gentoo recovery USB remained the running system.
- No reboot, boot result, service-health result, or physical acceptance is
  claimed.

* Gate 3A Storage Result
- Target: Sandisk Optimus 5100 1TB, serial =26133D807390=.
- Power precondition: AC online and battery at 98 percent.
- GPT disk GUID: =EBDAABD5-55FF-4DC2-874E-6E40ADD378E3=.
- ESP: sectors =2048-2099199=, FAT32 UUID =878D-A749=, mounted =/mnt/boot=.
- Swap: sectors =2099200-69208063=, UUID
  =28d5c812-d3e4-4ba2-8cd0-535a35162ab6=, active.
- Btrfs: sectors =69208064-1953525134=, UUID
  =995d2695-879e-4577-aaaf-9fed26eed6f4=.
- =@root= mounted at =/mnt= and =@home= at =/mnt/home= with
  =compress=zstd,noatime=.
- Recovery USB remained =/dev/sda=; no install, bootloader write, EFI entry,
  activation, or reboot occurred.
- The pinned =nixos-generate-config= closure was copied transiently to the live
  image and generated the hardware module from these real mounts.

* Gate 2 Build Result
The exact documented no-link/no-write-lock-file command evaluated and built:

#+begin_example
/nix/store/a1rglfnj8fpql25w301v5xwk05qvw266-nixos-system-t480s-26.11.20260729.0954f7e
#+end_example

The closure is 7.8 GiB.  Its boot artifacts reference stock Linux 6.18.40 and
its initrd.  Brave, Emacs PGTK, Ghostty, DWM, dmenu, slstatus, tmux, Fish,
Ranger, and nsxiv are present; Firefox, Neovim, Vesktop, VS Code, Steam,
Hyprland, and Quickshell are absent by exact closure-name inspection.  After
replacing three deprecated X utility aliases, an exact warning-clean rebuild
returned the same toplevel.  The lockfile remained byte-identical at
=8748aa09fa9f1ce1faafedcb855c84c9dd1be4913dcc96f01086a14b3aebfaf7=.
No activation or live-system change occurred.

* Historical Source-only Checks
These results belong to the Gate 1/Gate 2 source identified above, not the
later sanitation candidate.

- =git diff --check=: passed.
- =nixfmt --check flake.nix host/t480s/system/default.nix host/t480s/home/default.nix=:
  passed.
- =shellcheck config/x11/display-profile=: passed.
- T480s Nix source contains no Firefox, Neovim, =st=, Hyprland, Quickshell,
  Vesktop, VS Code, Steam, NVIDIA, PRIME, Razer, QMK, or Wayland-session
  package/import references.
- MSI source repository remained clean at =main...origin/main=.
- Gate 2 evaluation and realization passed; runtime remains untested.

Primary source SHA-256 values at the Gate 1 stopping point:

| Path | SHA-256 |
|------+---------|
| =flake.nix= | =c97c48cac464bfb851331075d717dcdd5bbaaeaaa11069b2f189052211dbf100= |
| =host/t480s/system/default.nix= | =a608827bd89e5e1cf82eac2e6d7d69831efe5b8075be59ca20671d4550fd4d24= |
| =host/t480s/home/default.nix= | =338b4c7ea2daddecc2aed6b88ad3b4e6eb0e54d9891c27652ff3119a8350e39c= |
| =config/dwm/dwm.h= | =6ef3cbeaa689fe8e3b97f095f90ba79ff594f4f6b162946814733e3a1aa1bdb4= |

* 2026-08-14 Static-check Status
Completed on the T480s against that checkpoint's working bytes:

- all 38 Nix files pass =nix-instantiate --parse= and =nixfmt --check=;
- Fish and display-profile shell parsing pass; display-profile is mode =0755=
  and its internal/external mock cases pass;
- all 28 Emacs Lisp files pass an isolated reader check;
- active import and live-link targets exist;
- the selected package multiset and moved system behavior match the pre-split
  source;
- Ranger =rc.conf= is hash-identical to T430 and =rifle.conf= differs only by
  the approved Firefox-to-Brave substitution;
- excluded-source and VS Code application scans pass, with only documented
  upstream comments and standalone =vscode-*= web language-server names;
- =git diff --check= and =git diff --cached --check= pass;
- =flake.lock= remains absent.

The first current-source build =qld2qxc5...= passed but was rejected before
activation because its =mpris-proxy= wants link targeted BlueZ's nonexistent
=share/= unit path.  The approved one-line =share= to =etc= adaptation produced
the replacement =d39b1zd7...= candidate.  Its native unit verifies and all Home
Manager links resolve.  The later Emacs-repair candidate and its activation are
recorded below; within that correction slice no persistent profile change,
reboot, commit or push occurred.

* 2026-08-15 Emacs Repair
The 2026-08-15 repair keeps Package.el and =emacs-sync=, =emacs-gtk= on X11,
the Iosevka Term role and the live =config/emacs= ownership.  It restores
Ledger, TeX Live Small, ImageMagick, All The Icons fonts and generated Eshell
aliases.  No desktop entry is added.  GLSL, QML, Haskell, Lazygit and Yazi
remain excluded.

Source/static validation, Package.el and vterm bootstrap, a non-activating Nix
build, candidate activation, daemon restart/runtime acceptance, cache
quarantine, commit and push are separate gates.  The later cache action is
limited to excluded QML, Haskell and GLSL package residue; it does not include
Magit, vterm, Ledger or retained packages.

Source/static validation passed for all 38 Nix files and all 28 Emacs Lisp
files.  The build-input staged-diff SHA-256 is
=40828ff88fe01c1d2b3621206b8e1f727759300f8d5d4fce7a2e6ab9e8d8a9a8=.
The user-cache bootstrap then refreshed Package.el, installed Magit and Ledger
mode, built vterm, and passed batch loading.  Its reflink rollback copy is
=~/.cache/emacs.before-repair-20260815=.  The daemon stayed at PID =146045=
with zero restarts.

The separately authorized non-activating build then exited zero and realized
=/nix/store/vwnm1y20943v5vqa96404mkhbq4zdnqg-nixos-system-t480s-26.11.20260813.0e251e2=.
Its 1,944-path closure reports 10.6 GiB and contains the approved Ledger,
ImageMagick, TeX Live, All The Icons fonts, generated aliases and X11 Emacs
artifacts.  The generated unit and links pass static inspection.  =flake.lock=
and =result= remain absent.

The exact candidate was then activated temporarily with
=switch-to-configuration test= and exited zero.  The persistent system profile
and booted fallback remain unchanged.  System and user managers are running
with no failed units; SSH, NetworkManager, Wi-Fi and =mpris-proxy= are healthy.
All prior rollback switch tools remain present.

Home Manager performed the predicted Emacs stop/start.  The replacement daemon
is active at PID =169304= with =Result=success=, zero restarts and no init-file
error.  Batch verification loaded Magit, vterm and Ledger mode.  Post-activation
checks found their commands and providers, the native vterm module, all 11
Eshell aliases, ImageMagick and TeX Live.  Ledger 3.4.1 is in the user profile,
all six All The Icons font files are live, and Fontconfig resolves Iosevka Term;
actual frame rendering remains physical acceptance.  The remaining GTK/X11
daemon warning is expected for the explicitly retained =emacs-gtk= choice, not
a PGTK/configuration defect.

Physical keybinding, icon rendering and interactive Magit/vterm/Ledger
acceptance remain user checks.  Promotion, reboot, cache quarantine and push
remain separate; the later root commit is recorded in the gate ledger.

* Known Deferred State
- No XLibre overlay.
- No specialized kernel or ThinLTO.
- No independent backup target.
- UPower remains =PowerOff= at 5 percent until hibernation acceptance.
- External output name/mode remains a runtime calibration through
  =T480S_EXTERNAL_OUTPUT= and =T480S_EXTERNAL_MODE=.
- Fingerprint authentication is accepted for sudo only with password fallback;
  login and other PAM surfaces remain disabled.  Browser/app passkeys are
  paused pending a mature Linux platform-authenticator design.
- Dormant Home modules are GnuPG and removable-media automation.  Dormant
  system modules are package candidates,
  input extras and removable-media automation.  Exact paths are listed in
  [[file:tree-design.org][the ownership map]].
- User/groups/SSH/sudo/Fish-shell policy remains inline in the host system root.
  A structural move needs separate exact review because it contains access and
  privilege policy; behavior is unchanged.

* Recovery And Preservation
- The current authoring checkout is the T480s repository itself; its origin is
  =https://github.com/skydive420dz/t480s-dotfiles.git=.
- The former pre-sanitation source snapshot at
  =/tmp/t480s-source-sanitize-pre.quhO0b.tar.gz= is no longer present.  At
  creation it was mode =0600=, excluded =.git=, and had SHA-256
  =f6483906c94a4edd7d65c4a4676d6080d01c199ba12aa5d2c596456e8f7db52b=.
  It is historical provenance, not a current recovery artifact.
- The previous inherited tip =3d96cc40e9b1ba1ec3385245862ee13e45f3dfdd= is
  not an ancestor of the standalone root commit.  It is no longer =origin/main=;
  the recorded hash and any retained local reflog are its recovery evidence.
- Generation 10 =q80fa9...= is current, profile-selected and booted; generation 9
  =ysxm6ik...= remains the retained fallback.
- The accepted Yazi slice progressed through test activation, persistent boot
  staging and one reboot.  It did not touch MSI, repartition storage, regenerate
  hardware configuration or change =flake.lock=.
- Roots queries automatically pruned stale auto-GC and temporary-root metadata;
  no live root, profile, candidate or service changed.

* Resume Procedure
1. Read this file and [[file:gates.org][the gate ledger]].
2. Verify the T480s identity, repository path, =main=, tracked =flake.lock= and
   equality with live =origin/main=.
3. Stop on unexplained source or live-state drift.
4. Verify generation 10 =q80fa9...= as current, booted and profile-selected and
   retain generation 9 =ysxm6ik...= as rollback.
5. Treat Gate 5B and the generation 7 paths as historical evidence only.
6. Reopen browser WebAuthn only after a new security and fallback review.

* Next Unauthorized Action
No Yazi deployment work remains.  Any later source, build, activation, reboot
or hardware change requires a new gate.  Browser WebAuthn remains deferred.
