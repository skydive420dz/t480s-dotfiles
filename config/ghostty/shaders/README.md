# Ghostty Shaders

- `cursor_blaze.glsl` and `cursor_smear.glsl` come from KroneCorylus' Ghostty shader playground.
- `bloom025.glsl` is a local subtle bloom variant adapted from qwerasd205's bloom shader.
- `LICENSE.KroneCorylus` covers the imported playground shaders.
