;;; sk-solaire.el --- Secondary buffer background depth -*- lexical-binding: t; -*-

(use-package solaire-mode
  :demand t
  :config
  (solaire-global-mode 1))

(provide 'sk-solaire)

;;; sk-solaire.el ends here
